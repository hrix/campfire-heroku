- flash.each do |key, value|
  .alert.alert-dismissable{class: "#{bootstrap_class_for(key)} fade in"}
    %button.close{"data-dismiss" => "alert", :type => "button"} ×
    %strong #{key}
    = value
